# Ubuntu Server Lab

## What I built
Deployed Ubuntu Server 22.04 LTS in VirtualBox as the foundation 
of a home lab environment.

## Steps completed
- Installed Ubuntu Server 22.04 LTS
- Updated system packages via apt
- Installed and configured Apache web server
- Verified web server accessible via port forwarding (host port 8080)

## Screenshots
![Apache default page](screenshots/apache.png)

## Skills demonstrated
Linux CLI, package management, web server config, VM networking
```

---

## Step 7 — Push everything to GitHub

Back in Git Bash:
```
git add .
git commit -m "Add Ubuntu Server lab with Apache setup"
git push origin main